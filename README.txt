Run: npm install && npm run dev
This is a testing package with login, dashboard, OCR mock/parser, history, trends, profile, medications, and multi-user preview.
Next planned steps: real camera OCR, Firebase auth and storage.
